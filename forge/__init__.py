"""
forge package — The Forge autonomous development orchestrator modules.

Modules:
    forge_config    — constants, paths, env vars
    forge_log       — logging, timestamps, terminal formatting
    forge_repos     — repository registry, branch management
    forge_state     — state persistence, task DAG, scheduling
    forge_git       — git operations
    forge_discord   — Discord client, PDF, message formatting, approvals
    forge_opencode  — OpenCode subprocess, log parsing, agent sync
    forge_prompts   — task prompt builders, report file helpers
    forge_runner    — execute_task() full cycle
"""